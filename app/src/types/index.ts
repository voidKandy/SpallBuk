export type Prompt = {
  uuid: string,
  name: string,
  description: string,
  prompt: string,
};

export type User = {
  name: string,
  uuid: string,
}

export type Session = {
  name: string,
  uuid: string,
}
